#include "main.h"
#include <stdio.h>
/**
 * add - two integers and returns the result
 * @a: first input
 * @b: second input
 * Return: Sum of a & b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
