#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiplies two integers
 * @a: the first number to be checked
 * @b: the sec number to be checked
 * Return: the result of multiplication a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
